# 构建参数配置示例
# 复制此文件为 .env 并根据需要修改参数

# 版本信息 (可选，默认从git获取)
# VERSION=v1.0.0

# Git提交哈希 (可选，默认从git获取)
# COMMIT=abc1234

# 构建时间 (可选，默认为当前时间)
# BUILD_TIME=2024-01-20 10:30:00 UTC

# 数据库配置
DB_HOST=postgres
DB_PORT=5432
DB_NAME=vgo_iam
DB_USER=vgo_user
DB_PASSWORD=vgo_password

# Redis配置
REDIS_ADDR=redis:6379

# 日志级别
LOG_LEVEL=info